export default function cancelFileBtnStyle() {
  const headingStyle = `
                        h-[15px] mt-1 p-1 bg-bright-red
                        text-gray-light
                        rounded-full leading-[5px]
                        hover:text-white
                        hover:bg-bRed
                        active:bg-red
                      `;

  return {
    headingStyle,
  };
}
